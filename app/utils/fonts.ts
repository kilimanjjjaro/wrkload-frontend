import { Inter, Syne } from '@next/font/google'

export const inter = Inter({
  variable: '--inter-font',
  subsets: ['latin']
})

export const syne = Syne({
  variable: '--syne-font',
  subsets: ['latin']
})
